# 手触り / Tezawari

画一的で人工的に見える UI を避けるためのデザイン言語である。
温かみを色に求めない。
形の不揃いと、反応の局所性と、時間の非対称性に置く。

実装は [index.html](index.html) の単一ファイルにある。
依存はなく、ボタンと札と欄と織りの四部品が入っている。

## 温かみの置き場所

手仕事の器が工業製品と違って見えるのは、色ではなく形が理由である。
一つずつ縁の厚みが違い、線の太さが一定でなく、面に繊維が走っている。
握ったときの応えかたも違う。
押した場所がへこみ、離せば揺れて戻る。

この言語は、その差を三つの場所に置く。
輪郭の幾何と、線の引かれかたと、反応の時間である。
色は静かに保ち、紙と墨に近い階調だけを持つ。
彩度のある色は朱の一色だけで、訂正の印にしか使わない。

## 個体は不変である

不揃いであることと、毎回変わることは違う。
手で作られた器は一つずつ違うが、その一つは昨日も今日も同じ形をしている。
毎フレーム形が揺れ動くものは、手仕事ではなく雑に見える。

そこで輪郭のゆらぎは擬似乱数で作り、その種をラベル文字列と DOM 順から決める。
同じ指定の四つのボタンは四つとも違う輪郭を持ち、再読み込みしても各個体は同じ形に戻る。
塗り直したいときだけ、大域の salt を進めて全個体を焼き直す。

## 素材の原則

- **個体差**：同じ指定から二度同じ形は出ない。ただし一つの個体は不変である
- **筆圧**：線の太さは一定でない。同じ輪郭に太い破線を重ねて、墨の溜まりを作る
- **地肌**：面には繊維がある。異方性を持たせれば布の織りになる
- **耳**：大きい紙ほど縁は暴れる。裁たれていない縁には毛羽が立つ

## 動きの原則

- **気配**：手が近づいた時点で、触れる前に素材が応える
- **たわみ**：触れた一点が変形し、離すと減衰しながら戻る
- **息**：静止していても、完全には止まっていない
- **質量**：大きい物はゆっくり動き、ばねも弱い
- **節度**：反応するのは、触れられる物だけである

最後の一つは他の四つを制限する。
静的な札や入力欄は、近づいても傾かないし沈まない。
反応そのものが「これは触れる物だ」という合図になるので、触れられない物が反応すると嘘になる。

## 一つの塗師

ボタンも札も欄も織りも、同じ関数 `paint()` が描く。
違いは要素に載った CSS トークンだけである。

`paint()` は要素の実寸を測り、種から輪郭を生成し、殻（`.tz-shell`）に SVG を書き込む。
殻は要素の外側 `--tz-pad` ピクセルまではみ出せるので、影も耳も焦点環も境界箱の外に描ける。

### 層の順序

どの部品も同じ順で塗られる。
不要な層は CSS で `display:none` にする。

| 層 | 役割 | 使う部品 |
|---|---|---|
| `tz-shade` | 接地影。ぼかした同一輪郭 | 札 |
| `tz-fill` | 地 | ボタン、枡、織り |
| `tz-press` | 指の位置から広がる凹みの陰 | ボタン、札 |
| `tz-grain` | 地肌の繊維 | ボタン、札、織り |
| `tz-edge2` | 筆圧。太い破線を輪郭に重ねる | ボタン、札 |
| `tz-soak` | 記入の染み | 欄 |
| `tz-edge` | 輪郭そのもの | 全部 |
| `tz-correct` | 朱の波線 | 欄 |
| `tz-hook` | かぎ。書ける範囲の両端 | 欄 |
| `tz-fringe` | 耳の毛羽 | 札 |
| `tz-ring` | 焦点の環 | ボタン、札 |

輪郭は `<defs>` に一本だけ置き、七つの層が `<use>` で参照する。
変形は参照元の `d` を一度書き換えれば全層に伝わる。
焦点環だけは別の輪郭を参照する。
たわんだときに焦点表示まで動くと、位置が読めなくなるためである。

### 二階層のゆらぎ

輪郭は二つの尺度でゆらぐ。

幾何のゆらぎは、角丸矩形の周を等間隔で標本化し、各点を法線方向に周期ノイズで押し出して作る。
ノイズの周波数は整数倍に保つ。
閉じた輪郭が継ぎ目なくつながるためには、周期が一周で割り切れる必要がある。
`--tz-grit` はこの周波数の倍率で、上げるほど細かい破れが混ざる。

繊維のゆらぎは SVG の変位フィルタで与える。
種は個体ごとに違うので、同じ寸法の部品が並んでも同じ歪みかたはしない。

幾何が個体の歪み、フィルタが繊維の粗さを担う。
この二重の尺度が、単なる歪んだ矩形と手漉きの紙を分ける。

## トークン

既定値は `:root` にあるが、部品は必ず自分で上書きする。
カスタムプロパティは継承するので、札の中に置いたボタンが札の粗さを受け継いでしまうためである。

### 作りのトークン

| トークン | 意味 | ボタン | 札 | 欄 | 織り |
|---|---|---|---|---|---|
| `--tz-pad` | 輪郭が紙からはみ出せる余白 | 10 | 24 | 12 | 8 |
| `--tz-amp` | 個体差の振れ幅 (px) | 1.05 | 2.05 | 0.85 | 1.7 |
| `--tz-grit` | 縁の粗さ（周波数の倍率、整数） | 1 | 3 | 2 | 4 |
| `--tz-fiber` | 繊維レベルのゆらぎ | 1.15 | 2.1 | 0.9 | 1.6 |
| `--tz-radius` | 角丸 | 13 | 7 | 6 | 5 |
| `--tz-grain-freq` | 地肌の目 | 0.85 | 0.7 | 0.8 | 0.02 0.62 |
| `--tz-form` | 輪郭の種類 | box | box | rule | box |

織りの `--tz-grain-freq` だけが二値を持つ。
横方向の周波数を下げ縦を上げると、等方な粒子が横向きの繊維になり、布に見える。

`--tz-form` は輪郭の種類を選ぶ。
`box` は閉じた輪郭、`rule` は下端を走る一本の開いた罫、`ruled` は面の中に行数分の罫を引いた原稿用紙である。

標本化の間隔は `--tz-grit` から導く（`clamp(8 / √grit, 3.2, 8)`）。
粗い縁ほど高い周波数を含むので、それを解像できるだけ細かく標本化する必要がある。

### 反応のトークン

| トークン | 意味 | ボタン | 札 | 拾える札 | 欄 |
|---|---|---|---|---|---|
| `--tz-dent` | たわみの深さ (px) | 3.6 | 2.3 | 2.3 | 0 |
| `--tz-sigma` | たわみの広がり（短辺比） | 0.55 | 1.7 | 1.7 | 1 |
| `--tz-sigma-min` | 広がりの下限 (px) | 26 | 120 | 120 | 40 |
| `--tz-curl` | 角の起き上がり (px) | 0 | 0 | 3.4 | 0 |
| `--tz-tilt` | 気配で傾く角度 (deg) | 1.9 | 0 | 1.1 | 0 |
| `--tz-reach` | 気配が届く距離 (px) | 160 | 0 | 150 | 130 |
| `--tz-arrive` | 手が着いたら平らに戻る距離 | 26 | 0 | 0 | 0 |
| `--tz-release-dur` | 戻りの時間 | 540ms | 760ms | 760ms | 420ms |

`--tz-sigma` がボタンと札を分ける。
ボタンは短辺の 0.55 倍なので、押した一点だけが局所的にへこむ。
札は 1.7 倍なので、一点がへこむのではなく一枚がしなる。
実測すると、幅 668px の札の左端を押したとき、手前の縁が 2.29px、反対側の縁が 1.72px 動く。
同じ操作でボタンは手前 3.5px、反対側 0.0px である。

## 部品

### ボタン

形づくられた器である。
塗り（`--ink`）、輪郭（`--contour`）、素地（`--bare`）の三態を持つ。
素地は手を伸ばしたときに線が引かれ、それまで輪郭を持たない。

押すと、指の位置を中心にガウス減衰する重みで輪郭が内向きに沈む。
同時に押した辺が奥へ傾き（最大 2.1 度）、変形の原点が指の位置に移る。

状態は形で示す。
`disabled` は点線の下書き、つまりまだ墨の入っていない状態を表す。
読み込み中は縫い目が進む。
一定速度では進まず、引いて、止まって、また引く手縫いの拍を持つ。

### 札

漉かれた一枚の紙である。
縁は裁たれておらず、影は矩形のぼかしではなくその個体と同じ輪郭を持つ。

`--pick` は拾い上げられる札で、手にいちばん近い角が紙から起き上がる。
右上に手を寄せたとき、右上の角が 3.47px、隣の右下が 1.87px、対角の左下が 0.23px 動く。
押すと紙は机に押しつけられ、影のずれが 9.6px から 1px に縮み、濃度が上がる。

`--inlay` は浮かない札である。
影も耳も持たず、紙に沈んで区切るだけなので、近づいても何も起きない。

### 欄

書きこむ場所である。
触れる物ではなく、印を残す物なので、押しても沈まず傾かない。

押下のイベント処理そのものを付けていない。
包む要素でポインタを捕まえると、文字をドラッグして選べなくなるためである。
コード上も「押される物」と「気配だけ感じる物」を別の述語で判定する。

既定は枠ではなく罫である。
囲いが要るのは短い記号や数値のときだけで、そこでは枡（`--boxed`）を使う。
複数行は原稿用紙（`--ruled`）で、面の中に行の数だけ罫が引かれている。

#### 記入の染み

欄は書かれたことを憶える。
入力のたびに文字幅を canvas で実測し、書いた範囲だけ罫に墨を染ませる。

```
""        染み 0px
"山"       15.8px
"山田"      31.5px
"山田 太"    53.0px
"山田 太郎"   68.7px
```

カーソルを先頭に戻しても染みは残る。
墨は引っ込まないからである。

canvas の `measureText` は `letter-spacing` を計算に入れないので、字間は文字数から足す。

#### 穂先

1px の棒が方形波で明滅するのは、UI の中で最も人工的な部分である。
わずかに傾いた角の丸い穂に差し替え、明滅を非対称にした。
70% の時間は見えていて、10% の区間ですっと消え、24% かけてゆっくり戻る。
文字間を移動するときは 80ms かけて滑り、瞬間移動しない。

#### 変換中

日本語入力のための状態である。
`compositionstart` から `compositionend` までのあいだ、染みが点線になる。
まだ墨が定まっていないことを表す。

#### 朱

誤りは赤い枠ではなく、後から引かれる朱の波線で示す。
校正の朱入れと同じ扱いである。

罫そのものは墨のまま残す。
朱は加えられる印であって、紙を塗り替えるものではない。

### 織り

方向を持つ繊維の面である。
札の中で画像の代わりに使う。
異方性のノイズだけで作るので、外部の画像を持たない。

## 動きの実装

### var() 由来の transform は登録が要る

`transform` をカスタムプロパティで組み立てると、値を変えても補間されない。
登録されていないカスタムプロパティは補間可能な型を持たないためである。
トランジションは走らず、値が飛ぶ。

`@property` で型と初期値を宣言すると補間される。
実測で確認したばねの応答を挙げる。

```
押下  1 → 0.9867 (45ms) → 0.982 (125ms)
解放  0.982 → 0.9874 (60ms) → 0.9997 (240ms) → 1.00076 (420ms) → 1 (700ms)
```

解放の途中で 1 を超えて 1.00076 に達し、そこから戻る。
この行き過ぎが、緩んで戻る感触を作る。

### 押しは速く、戻りは遅い

押下は 90ms、解放は 540ms から 760ms かける。
物を押して離したときの弾性がそうなっているからである。

たわみの深さは時間の関数で与える。
押下は `D × (1 − e^(−t/0.032))` で 160ms 以内に沈みきる。
解放は `D × e^(−t/0.145) × cos(2π × 2.6t)` の減衰振動で、800ms で止める。

深さ 3.6px の極値は次のように推移する。

```
3.6px → −1.04px (167ms) → +0.27px (350ms) → −0.07px (533ms) → 0.02px (800ms)
```

負の値は外側への膨らみである。
戻りきらずに一度行き過ぎ、揺れが二度ほど見えてから収まる。

### 気配の強さ

ポインタと要素の矩形との距離 `d` から強さ `m` を決める。

```
m = (1 − d / reach)² × (arrive ? min(1, d / arrive) : 1)
```

前半は近いほど強くなる項である。
後半は手が着いたら平らに戻す項で、`--tz-arrive` が 0 の部品では働かない。

ボタンは `arrive` を 26 に設定してある。
近づく、着く、沈むの三段になり、傾きは接触の手前で最大になる。

```
d=160px  0.00度
d=100px  0.27度
d=60px   0.74度
d=26px   1.33度（最大）
d=10px   0.64度
d=0px    0.00度（手が着いた）
```

拾える札は `arrive` が 0 である。
角が起きたままでいてほしいからで、持ち上がるという合図を接触後も保つ。

欄は `--tz-tilt` が 0 なので、傾かずに罫の濃さだけが変わる。
同じ式から、部品ごとに違う現れかたを出している。

### 息

静止時も完全には止まらない。
殻だけが周期 13 秒から 22 秒、振幅 0.085 度と 0.38% で揺れる。
周期にも位相にも個体ごとの乱数を入れてあるので、並んだ部品が揃って呼吸することはない。

大きい物ほど遅い。
短辺から倍率（最大 2.2 倍）を出して周期に掛ける。

殻だけを動かし、ラベルは動かさない。
変形するのは `transform` だけなので合成で済み、ラスタライズが走らない。

### 生まれかた

部品ごとに違う。
ボタンは一本の線として引かれ、札は紙として置かれ、欄は罫が引かれる。
所要時間は個体ごとに 540ms から 960ms のあいだで散らし、46ms ずつずらして始める。

札は内容が着地してから読めるようになる。
子要素に添字を渡し、読む順に 85ms ずつ遅らせている。

### 使い込み

触れるたびに、地肌の粒子と線の太さがごくわずかに増える。
12 回で頭打ちにしてあるので、一回では気付かない。
使い続けた部品だけが馴染む。

## 焦点の示しかた

ボタンと札は、輪郭の外側に手描きの環を描く。

欄は囲まない。
薄い下書きの罫が、その場で本式の罫になることで示す。

| | 未焦点 | 焦点 |
|---|---|---|
| 罫 | 1.15px / 不透明度 0.38 | 2.2px / 1.0 |
| 枡と原稿用紙 | 1.15px / 0.38 | 1.9px / 1.0 |

線幅が 91% 増え、不透明度が 163% 増える。
フィールドの全幅にわたる変化なので、面積としては環より大きい。

罫のときは両端に**かぎ**が立ち上がる。
書ける範囲を示す 8px から 10px の跳ねで、囲わずに端だけを教える。
枠の実用的な役割は範囲を示すことだけなので、それだけを残した。

枡は引き直さない。
囲いは既にそこにあるので、濃くなれば足りる。
閉じた輪郭を引き直すと、途中の数フレームが破線に見えてしまう問題もある。

## アクセシビリティ

不完全さは輪郭と地肌だけに置く。
文字、当たり判定、コントラスト、焦点表示は崩さない。

焦点表示は常に可視で、たわみの影響を受けない別の輪郭を参照する。
状態は色だけに頼らない。
`disabled` は点線、読み込み中は縫い目、誤りは朱の波線と注記と `aria-invalid` の三つで示す。

`prefers-reduced-motion` では、たわみ、気配、息、描き出し、穂先の明滅を止める。
個体差と筆圧と地肌は残る。
これらは動きではなく形だからである。

## 性能

輪郭のパス文字列は一個体につき一本だけ持つ。
七つの層が `<use>` で参照するので、重複を消して 716KB から 221KB になった。
変形時の書き換えも七回から一回に減る。

毎フレーム再計算されるのは、押されている一個体だけである。
気配と息は `transform` と不透明度しか変えないので、合成だけで済む。

画面の外にある個体は `IntersectionObserver` で息を止める。
タブが隠れて `requestAnimationFrame` が止まった場合に備えて、解放から 900ms 後に強制的に平らへ戻す経路と、`visibilitychange` で片付ける経路を持つ。

寸法が測れない要素（`display:none` の親を持つ場合など）は塗らずに飛ばす。
`ResizeObserver` が可視化を検出したときに塗り直される。

## 部品を足すとき

1. クラスに作りのトークンと反応のトークンを全部書く。既定値の継承に頼らない
2. `--tz-form` で輪郭の種類を選ぶ。既存の三種で足りなければ `paint()` に分岐を足す
3. 層の CSS は `> .tz-shell` で自分の殻に限定する。子孫セレクタで書くと入れ子の部品に漏れる
4. `paint()` が拾う要素の一覧にセレクタを加える
5. 触れられる部品かどうかを決める。触れられないなら `--tz-reach` を 0 にする

三番目は実際に踏んだ問題である。
`.tz-card .tz-shade` と書いたために、札の中に置いたボタンにまで影が付いた。

## 決めなかったこと

色で温かみを出すことはしない。
紙と墨の階調に留め、朱だけを訂正に使う。

輪郭を毎フレーム揺らすことはしない。
不揃いは手仕事に見えるが、揺れ続けるものは雑に見える。

浮き上がるホバーは使わない。
ボタンが浮くのはよくある動きなので、線に墨が入る変化に置き換えた。

矩形のぼかし影は使わない。
影は、その個体と同じ輪郭を持つ。
